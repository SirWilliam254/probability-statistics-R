#dbeta(x,alpha, beta)
#pbeta(x,alpha, beta)
#qbeta(Fx,alpha, beta)
#rbeta(numSims, alpha, beta)

# Considering a Beta random variable X with parameter ?? = 3 and ?? = 4.
# we can find say P(X < 0.3).
pbeta(0.3, 3, 4)
